<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="10dp"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:id="@+id/pop_up_window"
    android:background="@drawable/dialog_bg"
    >

    <EditText
        android:id="@+id/enter_root_view"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:background="@drawable/input_text_edit_text_shape"
        android:padding="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginHorizontal="8dp"
        android:textSize="18sp"
        android:textColor="@color/almost_black"
        android:textColorHint="@color/gray"
        />

    <TextView
        android:id="@+id/fab_approve"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/enter_root_view"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="Add"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="32dp"
        android:layout_marginEnd="16dp"
        android:textColor="@color/orange_primary"
        android:textSize="18sp"
        android:clickable="true"
        />

    <TextView
        android:id="@+id/fab_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/enter_root_view"
        app:layout_constraintEnd_toStartOf="@id/fab_approve"
        android:text="Cancel"
        android:layout_margin="16dp"
        android:textColor="@color/orange_primary"
        android:textSize="18sp"
        android:clickable="true"
        />

</androidx.constraintlayout.widget.ConstraintLayout>